server:
  port: 8081

spring:


  # Database connection settings
  datasource:
    url: jdbc:mysql://localhost:3306/fitnessuserservice
    username: root
    password: vedant@555
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Hibernate settings
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
